{
  "name": "@scaffold-eth/subgraph",
  "license": "UNLICENSED",
  "version": "1.0.0",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ GITHUB_USERNAME/your-contract",
    "clean": "sudo rm -rf ./../../../data",
    "create-local": "graph create --node http://localhost:8020/ dao-strategies/campaign",
    "remove-local": "graph remove --node http://localhost:8020/ dao-strategies/campaign",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 dao-strategies/campaign"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.26.0",
    "@graphprotocol/graph-ts": "^0.24.1"
  },
  "devDependencies": {
    "mustache": "^4.2.0"
  }
}
